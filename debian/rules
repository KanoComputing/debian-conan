#!/usr/bin/make -f

%:
	dh $@ --with python3

override_dh_auto_install:
	pip3 install --requirement requirements.txt --root `pwd`/debian/python-conan

override_dh_python3:
	dh_python3 --shebang=/usr/bin/python3
